package com.yyy.dailycode.stock.scheduler;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.annotation.Lazy;
import org.springframework.stereotype.Component;

@Component(TaskTimeoutScheduler.BEANID)
@Lazy(value = false)
public class TaskTimeoutScheduler {

	private Logger logger = LoggerFactory.getLogger(TaskTimeoutScheduler.class);
	
	public static final String BEANID = "taskTimeoutScheduler";
	
	/**
	 *  碎片一录/二录/三录任务超时处理 通知流程平台之后 要释放掉该任务
	 *  
	 */
	public void run(){
		/*增加系统配置，配置打开后监控任务超时*/
		//BprsService bprsService = SpringContextHolder.getBean(BprsService.BEANID);
		logger.info("fffffff");
//		MulthreadFinanialIndicatorImport m = new MulthreadFinanialIndicatorImport();
//		m.dealListWithMutiThread();
			
	}
	public void run1(){
		/*增加系统配置，配置打开后监控任务超时*/
		//BprsService bprsService = SpringContextHolder.getBean(BprsService.BEANID);
		logger.info("run1");
//		MulthreadFinanialIndicatorImport m = new MulthreadFinanialIndicatorImport();
//		m.dealListWithMutiThread();
			
	}
}
